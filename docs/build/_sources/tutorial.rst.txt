Tutorial
========

The *cage* package has a useful command line interface (CLI) which is accessible once you install the package using pip install. In order to see more information on the use of this CLI, just type:

	cage --help

into the command prompt.
